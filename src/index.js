import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// this is what a component looks like when you want to use it.
ReactDOM.render(<App/>, document.getElementById("root")) // it is passed as JSX element

// React can only render one application.
